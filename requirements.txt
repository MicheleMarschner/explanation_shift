# --- Core numeric ---
numpy==2.0.2

# --- PyTorch stack (pin as a matched trio) ---
torch==2.9.0
torchvision==0.24.0
torchaudio==2.9.0

# --- Model zoo / checkpoints ---
timm==1.0.24
huggingface_hub==1.4.1
safetensors==0.7.0

# --- Captum runtime deps (install explicitly since Captum will be --no-deps) ---
matplotlib==3.10.8
tqdm==4.67.3
packaging==26.0

quantus==0.6.0
